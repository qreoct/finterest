//Custom types for easier storage of data for authentication fields

//Used in login form
export interface LoginType {
	email: string;
	password: string;
}

//Used in registration form
export interface RegistrationType {
	email: string;
	password: string;
}